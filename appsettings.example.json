{
  "AI": {
    "ProjectEndpoint": "https://YOUR-AZURE-OPENAI-RESOURCE-NAME.openai.azure.com/",
    "ModelName": "YOUR-AZURE-OPENAI-DEPLOYMENT-NAME",
    "APIKey": ""
  },
  "KeyVault": {
    "_Comment": "Key Vault is not currently used. Leave Endpoint empty until a service explicitly requires stored secrets.",
    "Endpoint": ""
  }
}

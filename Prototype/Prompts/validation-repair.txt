The draft failed grounding validation: {{validationError}} Repair the JSON once using exactly the same evidence packet. Return the full corrected object as one concise, flowing conversation: keep the direct answer first, keep every claim relevant to the user's actual question, and do not use report headings or phrases such as "in this packet." Every claim must follow directly from its cited source; label a limited analogy as an analogy and do not import a ruling from another legal subject. For every quotation, copy one contiguous character-for-character span directly from the identified evidence text. Preserve its spelling, capitalization, punctuation, brackets, footnote markers, quotation marks, and word order; do not insert ellipses or join separate phrases. Verify each quotation is a literal substring before returning it. Do not add claims, evidence IDs, repeated material, a closing notice, or unsupported source relationships.

# Tanakh raw data

Versioned Sefaria JSON for Prophets and Writings is stored here. Torah is kept in the sibling `Torah` directory to avoid duplicate files; consult `../Metadata/manifest.jsonl` before ingestion.
